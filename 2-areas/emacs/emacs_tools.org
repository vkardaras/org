#+title: Emacs tools

* vterm

Prerequisites for installing vterm

#+begin_src sh
  # for arch
  sudo pacman -S base-devel cmake

  # for Debian/Ubuntu
  sudo apt install build-essential cmake libvterm-dev
#+end_src
