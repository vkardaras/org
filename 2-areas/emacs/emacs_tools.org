#+title: Emacs tools

* vterm

Prerequisites for installing vterm

#+begin_src sh
  # for arch
  sudo pacman -S base-devel
  sudo pacman -S cmake

  # for Debian/Ubuntu
  sudo apt install build-essential
  sudo apt install cmake
#+end_src
